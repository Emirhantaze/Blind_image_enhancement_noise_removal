The final code is the main.py in the root directory.

It is pretty simple to use. Run the main.py in your favorite shell and drop and drag the 
noisy image and thats all the recovered image will be saved in the root folder named as out.png
